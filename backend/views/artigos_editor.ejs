<!DOCTYPE html>
<html lang="PT-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Artigos</title>
  <style>
    :root {
      --font-family: 'Archivo', sans-serif;
      --h1: 800 24px/135% var(--font-family);
      --text: 400 16px/140% var(--font-family);
      --cor-principal: #291B1A;
      --cor-secundaria: #5ca3b3;
      --cor-fundo: #F0E8C2;
      --cor-body: #0F172A;
    }
  #titulo_artigo   { grid-area: titulo_artigo; }
  #conteudo_artigo { grid-area: conteudo_artigo; }
  #id_categoria    { grid-area: id_categoria; }
  #id_autor        { grid-area: id_autor; }
  #alt_imagem      { grid-area: alt_imagem; }
  #imagem_artigo   { grid-area: imagem_artigo; }
  #resumo_artigo   { grid-area: resumo_artigo;}
  #data_publicacao {grid-area: data_publicacao}
  #destaque        {grid-area: destaque}
  .btn-group       { grid-area: btn-group; }

    body {
      background-color: var(--cor-body);
      font-family: Arial, Helvetica, sans-serif;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      min-height: 100vh;
      padding: 2rem;
    }

    .container {
      border: 1px solid var(--cor-principal);
      color: var(--cor-principal);
      padding: 2rem;
      background-color: var(--cor-fundo);
      width: 60rem;
      min-height: 50vh;
      display: flex;
      flex-direction: column;
      gap: 2rem;
      box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.1);
      border-radius: 0.8rem;
    }

    h1 {
      font: var(--h1);
      text-align: center;
      font-size: 2rem;
    }

    .fonth1 {
      font: var(--h1);
    }

    table {
      border-collapse: collapse;
      width: 100%;
    }

    table, th, td {
      border: 1px solid black;
      padding: 0.5rem;
      text-align: center;
      vertical-align: middle;
      background-color: aliceblue;
    }

    .button-td {
      background-color: var(--cor-principal);
      color: #fff;
      border: none;
      border-radius: 0.5rem;
      padding: 8px 16px;
      font-size: 0.9rem;
      font-weight: bold;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.2s ease;
      box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
      display: inline-block;
    }

    .button-td:hover {
      background-color: var(--cor-secundaria);
      transform: scale(1.05);
    }

    .button-td:active {
      transform: scale(0.95);
      box-shadow: 0px 1px 2px rgba(0, 0, 0, 0.2);
    }

    .btn-delete {
      background-color: #e74c3c;
    }
    
    .btn-group {
      display: flex;
      gap: 1rem;
      margin-top: 1rem;
      align-items: center;
      align-content: center;
    }
     
   .form-principal{
    display: grid;
    grid-template-columns: 2fr 1fr;
    grid-template-areas:
    "titulo_artigo id_categoria"
    "resumo_artigo id_autor "
    "alt_imagem imagem_artigo"
    "data_publicacao destaque"
    "conteudo_artigo conteudo_artigo"
    "btn-group btn-group"; 
    gap: 3rem;
   align-items: start;
}
        
     
      .form-principal label {
        font-weight: bold;
      }
      .form-principal input, .form-principal textarea {
        padding: 0.5rem;
        border: 1px solid #ccc;
        border-radius: 0.4rem;
        font-size: 0.9rem;
        width: 100%;
        box-sizing: border-box;
        margin-top: 1rem;
      }
      .form-principal textarea {
        resize: vertical;
        min-height: 8rem;
        
      }

      #imagem_artigo input {
        background-color: #fff;
       
      }


      
      select {
        margin-top: 0.8rem;
        padding: 0.5rem;
        border: 1px solid #ccc;
        border-radius: 0.4rem;
        font-size: 0.9rem;
        width: 100%;
        box-sizing: border-box;
      }
  </style>
</head>
<body>

  <div class="container">
    
    
    <h2 class="fonth1">Cadastrar / Atualizar Artigo</h2>
    
    <main>
  <form class="form-principal" method="Post" action="/artigos/cadastrar_artigo">
  <input type="hidden" name="id_artigo" id="id_artigo">

  <div id="titulo_artigo">
    <label for="titulo_input">Título</label>
    <input id="titulo_input" type="text" name="titulo_artigo" required>
  </div>
     
   <div id="resumo_artigo">
    <label for="resumo_artigo_input">Resumo</label>
    <textarea name="resumo_artigo" id="resumo_artigo_input"></textarea>
    
   </div>
  
   
  <div id="alt_imagem">
    <label for="alt_imagem_input">Descrição da imagem</label>
    <input type="text" name="alt_imagem" id="alt_imagem_input">
  </div>
  <div id="imagem_artigo">
    <label for="imagem_artigo_input">Arquivo da imagem</label>
    <input type="file" name="imagem_artigo" id="imagem_artigo_input">
  </div>

  <div id="conteudo_artigo">
    <label for="conteudo_input">Conteúdo</label>
    <textarea id="conteudo_input" name="conteudo_artigo" id="conteudo_artigo_input"  required></textarea>
  </div>

  <div id="id_categoria">
    <label for="categoria_select">Categoria</label>
    <select id="categoria_select" name="id_categoria">
      <option disabled selected>Selecione uma categoria</option>
      <% categorias.forEach(categoria => { %>
        <option value="<%= categoria.id_categoria %>"
          data-id="<%= categoria.id_categoria %>">
          <%= categoria.nome_categoria %>
        </option>
      <% }) %>
    </select>
  </div>

  <div id="id_autor">
    <label for="autor_select">Autor</label>
    <select id="autor_select" name="id_autor">
      <option disabled selected>Selecione um autor</option>
      <% autores.forEach(autor => { %>
        <option value="<%= autor.id_autor %>"
        data-id="<%= autor.id_autor %>">
        <%= autor.nome_autor %></option>
      <% }) %>
    </select>
  </div>

  <div id="data_publicacao">
    <label for="data_select">Data publicação</label>
    <input type="date" id="data_select">
  </div>
  
  <div id="destaque">
   <label for="destaque_input">Destaque</label>
   <select name="destaque" id="destaque_input">
    <option value="1">Ativado</option>
    <option value="0">Desativado</option>
   </select>
  </div>

  <div class="btn-group">
    <button type="submit" class="button-td" name="action" value="incluir">Cadastrar</button>
    <button type="submit" class="button-td" name="action" value="atualizar">Atualizar</button>
  </div>
</form>
 
</main>

    <h1>ARTIGOS</h1>

    <table>
      <thead class="fonth1">
        <tr>
          <th>ID</th>
          <th>Título</th>
          <th>Resumo</th>
          <th>Conteudo</th>
          <th>Imagem</th>
          <th>Categoria</th>
          <th>Autor</th>
          <th>Ações</th>
        </tr>
      </thead>
      <tbody>
        <% artigos.forEach(artigo => { %>
        <tr>
          <td><%= artigo.id_artigo %></td>
          <td><%= artigo.titulo_artigo %></td>
          <td><%= artigo.resumo_artigo %></td>
          <td><%= artigo.conteudo_artigo %></td>
          <td>
            <img src="<%= artigo.imagem_destaque_artigo %>" alt="<%= artigo.alt_imagem %>" style="max-width: 100px;">
          </td>
          <td><%= artigo.nome_categoria %></td>
          <td><%= artigo.nome_autor %></td>
          <td>
            <button type="button" onclick="editar_formulario(
              '<%= artigo.id_artigo %>',
              '<%= artigo.titulo_artigo %>',
              '<%= artigo.resumo_artigo%>',
              '<%= artigo.conteudo_artigo %>',
              '<%= artigo.alt_imagem %>',
              '<%= artigo.id_categoria %>',
              '<%= artigo.id_autor %>'
            )" class="button-td">Editar</button>

            <form method="post" action="/artigos/deletar_artigo" class="form-inline">
              <input type="hidden" name="id_artigo" value="<%= artigo.id_artigo %>">
              <button type="submit" class="button-td btn-delete">Deletar</button>
            </form>
          </td>
        </tr>
        <% }) %>
      </tbody>
    </table>

  
  </div>

  <script>
    function editar_formulario(id_artigo, titulo_artigo, resumo_artigo, conteudo_artigo,  alt_imagem, id_categoria, id_autor) {
      
      document.getElementById("id_artigo").value = id_artigo;
      document.getElementById("titulo_input").value = titulo_artigo;
      document.getElementById("resumo_artigo_input").value = resumo_artigo;
      document.getElementById("conteudo_input").value = conteudo_artigo;
      document.getElementById("alt_imagem_input").value = alt_imagem;
      document.getElementById("categoria_select").value = id_categoria;
      document.getElementById("autor_select").value = id_autor;
      
     
    }
  </script>

</body>
</html>